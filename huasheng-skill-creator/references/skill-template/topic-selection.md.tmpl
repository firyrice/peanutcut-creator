# 选题

> 本模块可单独唤起：说"帮我想几个选题""{{NICHE}}选题"即可，无需走完整流程；也可作为完整链路第一步，选题确认后进入标题。

面向受众：{{AUDIENCE}}（{{PLATFORMS}}）。选题前先确认自己没有偏离账号人设——{{PERSONA}}，所有候选选题都要是这个身份会讲、且擅长讲的话题，不要产出一个通用账号也能发的选题。

## 前置：查历史内容库（避免重复 + 找系列线索）

产出候选选题前，先查一遍本账号的历史内容库，让新选题不撞车、能接上系列：

```bash
SKILL_DIR="$HOME/.claude/skills/{{ACCOUNT_SLUG}}-workflow"
[ -d "$SKILL_DIR" ] || SKILL_DIR="$HOME/.codex/skills/{{ACCOUNT_SLUG}}-workflow"
# 按本次方向的关键词查是否做过类似选题
python3 "$SKILL_DIR/scripts/query_db.py" --search "关键词"
# 若在做某个系列,列出该系列已产出的各期
python3 "$SKILL_DIR/scripts/query_db.py" --series "系列名"
```

- **去重**：命中的历史选题里,已经讲透的角度不要再重复;可以做"上次没展开的那一面"或"有新增量后的更新版"。
- **系列性**：若本次属于某个系列,先看已做过哪几期,新一期要和既有各期形成递进或互补,并在存档时带上同一个系列名。
- 库为空(新账号第一次用)或无法运行脚本时,跳过本步骤,照常产出选题。

## 第零步：先爬一圈热榜/热搜（强制，不可跳过）

**在列任何选题之前，先联网爬一圈当下热点。** 道理很简单：不查已有信息，根本不可能凭空知道今天有什么选题可做——真正的创作者都是先扫一圈热点、再从里面挑，而不是拍脑袋想。这一步是整个工作流的信息源头，跳过它，后面所有环节都是空中楼阁。

**怎么爬**：两条腿走路，先跑脚本拿结构化热榜，再用联网搜索补垂类角度。

**① 先跑热榜脚本（首选，拿全平台实时热榜）**：本技能自带 `fetch_hotlist.py`，调你自部署的 DailyHotApi，一次拿回抖音/B站/知乎/微博等平台的实时热榜——免登录、免污染、比裸搜干净得多。

```bash
SKILL_DIR="$HOME/.claude/skills/{{ACCOUNT_SLUG}}-workflow"
[ -d "$SKILL_DIR" ] || SKILL_DIR="$HOME/.codex/skills/{{ACCOUNT_SLUG}}-workflow"
# 扫本账号相关平台的实时热榜（平台按 {{PLATFORMS}} 调整）
python3 "$SKILL_DIR/scripts/fetch_hotlist.py" --platforms douyin,bilibili,zhihu,weibo --top 20
```

需先配好 `DAILYHOT_API_BASE`（见 `.env.example`）。**脚本抓不到会直接报错、绝不返回旧数据**——这是刻意的：偏离实时常识的选题就是垃圾，宁可停下修数据源，也不拿过时热榜硬编。

**② 再用 `scripts/web_search.py` 网关搜索补垂类角度**（别用自带 WebSearch）：热榜给的是"全网在聊什么"，还要针对 {{NICHE}} 挖"这个垂类当下的具体热点"，用 `python3 "$SKILL_DIR/scripts/web_search.py" "搜索词" --top 15` 扫下列源——

{{HOT_SOURCE_GUIDE}}

用带当前日期/"最新"/当前年份的关键词搜，确保是当下的，不要用训练记忆里的旧热点冒充实时情报。

**若热榜脚本和联网都不可用**：如实告知用户本次无法爬取实时热点，选题将只依赖下方垂类框架里的常青题产出，不含实时热点，绝不编造"当下热点"、也绝不拿旧数据冒充。

## 第一步：轻研究，筛出候选推荐给用户

爬完热点，挑出最有潜力的几个方向，**每条做一次轻研究**（不用深挖，够判断即可）：这条是不是真热点、能不能自然接上账号的选题框架、有没有一个像样的钩子、大致热度几何。轻研究的目的是**帮用户做选择**，不是把每条都写成完整方案——深度挖掘留到用户选定之后（见下方「与标题阶段的衔接」）。这样做也避免浪费：给未选中的方向挖尽资料没有意义。

一次产出 **N 个选题方案**（默认5个，可按用户要求调整数量），每个包含：

```
N. [选题标题] —— 选题池：[对应下方框架中的哪一类]
   结合的热点：[爬到的哪条热点，一句话；若为常青题则注明"非热点·常青"]
   钩子：[这个选题最抓人的那个点，一句话，用受众口语]
   预估热度：高/中/低 —— 理由：[命中的动机 + 比较优势 + 热点新鲜度]
```

全部产出后，按当前运行模式处理（模式在进入完整流程时已选定，见 SKILL.md「工作流程总览」）：
- **半自动模式**：**停下来**，用一句话请用户选定一个方向（或提出调整），例如："以上是{{NICHE}}方向的5个选题，你看想往哪个方向做，或者告诉我想调整的角度。定了我再深挖资料、起标题。"不要在用户选定前擅自往下走进入标题阶段。
- **全自动模式**：不停，按上面每条的"预估热度"和判断标准，AI 自己选最优的一条继续往下；把"选了哪条、其余备选、为什么选它"记下来，在最终交付时一并告知用户（留痕）。**例外**：若选定的这条命中政治/军事/监管/民族宗教等敏感方向，即便全自动也要停一次让用户确认后再深挖（见 SKILL.md 敏感题护栏）。

## 判断标准：动机、优势、反直觉

以上候选，每条在心里都要过下面三关（轻研究阶段用它们排热度、砍掉立不住的）：

### 受众动机
任何选题能不能被点开、看完，取决于它满足受众哪一类动机：
- **信息服务**：受众想通过这条内容解决一个具体问题、获取一个具体判断依据。质量标准是"看完之后受众能不能做出一个比看之前更清晰的判断"，不能只是堆信息量。
- **情绪价值**：受众想获得一种情绪上的满足或释放（共鸣、宣泄、被理解、爽感）。质量标准是这种情绪是否精准命中了受众心声——{{AUDIENCE_VOICE}}——而不是创作者自己想抒发的情绪。

按经验规律排序：**恐惧、愤怒、希望是驱动打开率最强的三种情绪动机**，其次才是好奇、猎奇、优越感、共鸣等较温和的动机。多数选题应该命中排序靠前的强动机。

### 比较优势
同一个话题市面上大概率已有账号讲过，选题能不能立住取决于有没有站得住的比较优势。逐一自检五种（更专业/更易懂/更有戏剧性/更有代入感/更有感染力），找出这条内容真正最突出的1-2个，而不是试图五个都占。判断标准：把这条内容和已知的头部同类内容并排放，能不能一句话说清楚"我比它强在哪"，说不清楚就换角度或放弃。

### 反直觉点
在同等信息量下，含反直觉点的选题更容易被打开：先找出受众对这个话题的**默认预期/常识印象**（必须真实存在、受众普遍持有，不能是自己发明的），再判断内容里是否存在一个和该预期相悖、但确实真实/可论证的信息点。反直觉点越具体、越可验证，讨论势能越强；站不住的反直觉点只会显得标题党。

## 垂类选题框架

{{TOPIC_FRAMEWORK}}

选题优先从上述框架里挑选和填充，而不是每次从零现想；框架内部保持轮换，避免同一个池子反复讲同一个具体案例或角度。定期回看各池分布，避免长期偏科。

## 热点结合是否牵强

结合热点能显著放大传播势能，但不是所有选题都要蹭，只用在"确实有交叉点、结合后不牵强"的场景。判断标准：把结合后的选题读给一个只关心本垂类内容的受众听，对方是否觉得这个连接自然。牵强蹭热点会损伤账号的专业感知度。

## 产出前自检

每个候选选题交付前都要能通过：

1. **动机明确**：一句话说清主要满足信息服务还是情绪价值，命中哪种情绪/需求
2. **优势清晰**：一句话说清相比同类内容的1-2个比较优势
3. **可持续性**：属于{{TOPIC_FRAMEWORK}}里的哪一类，不是孤立点子
4. **具体性**：不是泛泛的话题标签，必须具体到某个场景/判断/案例
5. **热点结合是否牵强**：若结合了热点，连接自然才保留

## 与标题阶段的衔接（重要）

本模块只对候选做**轻研究**。用户选定一条后，进入标题模块的第一步不是直接起标题，而是**对这条选定选题做深度素材挖掘**（拉若干篇相关报道/资料，提炼出最值得做标题的矛盾/戏剧点）——那一步才是起标题的真正依据。轻研究定方向、深挖定标题，两步分工明确：不要在轻研究阶段就把选中方向的资料挖尽（未选中的挖了也浪费），也不要在选定后跳过深挖直接套标题公式。
