---
name: {{ACCOUNT_SLUG}}-workflow
description: {{NICHE}}垂类的专属视频创作工作流，覆盖选题、文稿、标题、封面全链路，服务于{{PLATFORMS}}账号。当用户说{{TRIGGER_WORDS}}时使用。
---

# {{ACCOUNT_SLUG}}-workflow

这是一套为你的账号量身定制的视频创作工作流，覆盖从选题到文稿、标题、封面的完整链路，吃透了你所在平台的算法逻辑和垂类打法。

## 账号人设

{{PERSONA}}

所有选题、文稿、标题、封面都要贴合这个人设的身份和口吻来写，不要写成一个没有身份感的通用账号。

## 跨工具适配

本技能同时支持 **Claude Code** 和 **Codex**（以及其它遵循 SKILL.md 规范的 agent）。下文提到具体工具名时，请按你当前所在的运行环境映射到等价能力，不要因为"没有叫这个名字的工具"就跳过步骤：

- **联网搜索（统一走网关，别用自带 WebSearch）**：本技能的联网搜索**一律通过自带的 `scripts/web_search.py`（qianfan web search 网关）**，**不要**直接用 Claude Code 自带的 `WebSearch` 或 Codex 自带的联网搜索工具——这样搜索源可控、结果结构统一、跨 agent 环境行为一致。调用：`python3 "$SKILL_DIR/scripts/web_search.py" "搜索词" [--top N] [--json]`（密钥从技能目录 `.env` 的 `QIANFAN_WEBSEARCH_API_KEY` 读取）。若网关报错/无密钥，如实告知用户无法获取实时数据，退回内部知识作研究依据，不要用旧数据冒充实时情报，也不要偷偷改用自带 WebSearch。
- **抓取网页正文**：优先看 `web_search.py` 每条结果自带的 `content` 字段（网关已摘出约前 1000 字，判断相关性/快速扫风向足够）。需要读**完整全文**时用 `python3 "$SKILL_DIR/scripts/web_search.py" <url> --full [--max-chars N]` 本机直抓提纯——**不要用自带的 WebFetch**：它跑在云端，对国内站（163/百家号/东方财富等）会系统性报"无法确认域名安全"而失败，本机抓取无此问题。
- **写文件**：Claude Code 用 `Write` 工具；Codex 直接用其文件写入能力（或 `apply_patch`）。凡说"用 Write 工具生成某文件"，即指"用当前环境的文件写入能力落地该文件"。
- **跑脚本**：两个环境都通过 shell 执行 `python3`。脚本路径见下方「脚本路径」——一律用技能目录的绝对路径，不要假设当前工作目录就是技能目录。

### 脚本路径

本技能自身的脚本随技能一起安装，位于技能目录下的 `scripts/`。**不要假设 shell 的当前工作目录是技能目录**（Codex/Claude Code 运行时 cwd 通常是用户项目目录）。运行脚本前先定位技能目录，用绝对路径调用。技能目录按环境查找：

- Claude Code：`~/.claude/skills/{{ACCOUNT_SLUG}}-workflow`
- Codex：`~/.codex/skills/{{ACCOUNT_SLUG}}-workflow`

推荐用一句话解析出脚本根目录再调用，避免路径出错：

```bash
SKILL_DIR="$HOME/.claude/skills/{{ACCOUNT_SLUG}}-workflow"
[ -d "$SKILL_DIR" ] || SKILL_DIR="$HOME/.codex/skills/{{ACCOUNT_SLUG}}-workflow"
python3 "$SKILL_DIR/scripts/xxx.py"
```

下文示例为简洁起见写成 `scripts/xxx.py`，实际调用时请替换为 `"$SKILL_DIR/scripts/xxx.py"`。

## 前置步骤：背景信息采集（每次启动工作流时执行）

拿到选题或用户需求后，正式进入任何模块前，先执行：

0. **先确认今天的真实日期（不可跳过）**：不同 agent 的训练知识有截止日期，容易默认"今年"是知识截止的那一年，写出"XX年最新"这类一发布就露馅的错误时间表述。所以任何模块动笔前，先用系统时钟拿到真实日期——`date +"%Y-%m-%d"`（Windows 下 Codex/Claude Code 的 bash 同样可用）——并以这个真实日期为基准推算一切"今年/最近/当下/最新/去年"，绝不凭记忆里的年份想当然。（联网搜索用的 `web_search.py` 只解决"最近发生了什么"，解决不了"今天几号"——日期以系统时钟为准。）

1. 在以下信源中搜索与本次选题相关的近期内容（重点关注最近30天）：
   {{KEY_ACCOUNTS}}

2. 收集以下维度的信息：
   - 这些账号近期从哪些角度讲过这个话题
   - 评论区反应最强烈的点是什么（说明受众在意什么）
   - 有没有已经被反复讲烂的角度（说明这个角度已无增量价值）

3. 对照「已知常识层」区分两类信息：
   - **常识**（无需展开）：{{AUDIENCE_COMMON_KNOWLEDGE}} 中列出的内容
   - **增量**（重点展开）：受众在上述信源中尚未看到、或未被充分展开的角度

4. 把增量信息列表传递给选题和文稿模块，作为本次创作的核心方向。

若当前环境无法联网，跳过步骤1-2，直接依据 {{AUDIENCE_COMMON_KNOWLEDGE}}
推断增量方向，并告知用户本次未做实时信源查询。

## 工作流程总览

整个创作分四个模块（选题→标题→封面→文稿），走完后必过一道审查关，审查 PASS 再出分镜脚本交给花生 AI 成片。链路上原有两个人工决策点，现支持两种模式，**每次进入完整创作流程前先让用户选模式**：

**先选模式（用当前环境的交互选择能力，别只会打字问）**：
- **Claude Code / 有交互选项能力的环境**：用可点击的选项让用户选——`① 半自动（人在回路，每个决策点停下等你拍板）` / `② 全自动（一键出文，AI 自己拍板连续走到底）`。
- **无选项 UI 的环境（Codex / 裸模型等）**：用一句话问"这条走半自动（你逐步确认）还是全自动（我一口气做完）？"
- 生成本 skill 时可把常用默认写进来（{{ACCOUNT_SLUG}} 号默认模式）；用户也可在唤起时直接说"一键出文"跳过询问。

两种模式只改**人工决策点停不停**，不改质量关卡（审查两模式都强制）：

1. **[选题](references/topic-selection.md)** —— 基于垂类框架和热点产出候选选题。
2. **决策点1**：
   - **半自动**：停下，让用户从候选里选定一个方向（或提调整），确认后才继续。
   - **全自动**：不停，按选题模块的"预估热度/推荐"逻辑 AI 自己选最优一条，**并在最终交付时留痕**（写明"自动选了哪条、备选是什么、为什么选它"，方便用户事后回看拍得对不对）。
3. **[标题](references/title-craft.md)** —— 基于选定选题，产出爆款候选标题。
4. **[封面](references/cover-design.md)** —— 以选定标题为大字方向，生成对应平台的封面。
5. **决策点2**：
   - **半自动**：停下，让用户确认最终标题和封面方向，确认后才继续。
   - **全自动**：不停，AI 按模块的"推荐#N"选定标题/封面方向并留痕，直接进文稿。
6. **[文稿](references/script-writing.md)** —— 标题和封面已确定，基于承诺方向产出完整口播稿。
7. **[审查](references/script-review.md)（两模式都强制，不可跳过）** —— 文稿产出后必须走这一步：用"换一双眼睛"（独立审核 agent 或硬隔离复审）按三层清单逐条终审（硬门禁 / 受众复审 / 事实与立场终审），不过就打回重写、改完重审。任何存档/交付动作都以拿到审查 PASS 判决为前置条件。**全自动模式下没有人把关，这道关反而更重要——审查不通过就 AI 自己打回重写一轮，绝不因为"全自动"就放行未过审的稿子。**
8. **[分镜](references/storyboard-plan.md)** —— 审查 PASS 后，把确认的口播稿逐字拆成花生 AI 的分镜脚本，落成该视频文件夹下的 `storyboard_plan.md`（本工作流最终产出的是能直接成片的分镜，不是停在文字上的方案）。按 Method-B 组织：场景划分（=素材检索批次，只承载 `scene_design`）→ 分镜断句（口播原句逐字复用、`cloud_only`）→ **逐分镜给三档画面类型 + 画面描述**（一镜一给 b-roll / b-roll+mg / mg_frame，画面类型与描述定在分镜不是场景）→ **内嵌 JSON 输出**（`scenes[].shots[]`）→ 两层 MG 画风（固定基因 × 情绪温度 × 每期视觉母题，视觉母题从封面核心意象延续）。**硬护栏：所有分镜 `script` 按序拼接必须逐字等于口播稿全文，一字一标点不差。** 用户声明不成片时，走到审查 PASS 即停，不强制出分镜。本账号为真人出镜形态时，分镜另有 `a-roll` / `a-roll+mg` 两档（按露脸密度判定，详见分镜方法论）。

**全自动模式的敏感题护栏（唯一保留的强制停顿）**：若选定选题命中政治、军事、监管、民族/宗教等敏感方向，即便在全自动模式，也**必须停一次**让用户确认后再往下——无人把关下敏感题直接出稿风险过高，这一停不可省。

以上模块也**都可以单独触发**——比如你已经有现成的文稿只想起标题，或者已经定了标题只想生成封面，直接说明需求即可，不必每次都从选题走完整流程。（审查模块也可单独唤起：把一篇写好的稿子丢进来说"帮我按审查清单过一遍"；分镜同理：已有一篇现成口播稿只想要分镜，直接说"帮我拆分镜"，跳过前面的模块。）

若想了解这套工作流为什么是这样设计的（受众、差异化、变现路径、各平台适配依据），参见 [账号定位诊断](references/positioning.md)。

## 定位速览

{{POSITIONING}}

完整的定位依据、受众画像、差异化打法、变现路径见 [账号定位诊断](references/positioning.md)。

## 内容资产库

本工作流会把你每次产出的内容结构化沉淀到一个长期数据库,独立于本 skill 存放,重装/更新 skill 不会丢数据:

- **数据位置**：`~/.claude/content-db/{{ACCOUNT_SLUG}}/`（Codex 下 `~/.codex/content-db/{{ACCOUNT_SLUG}}/`）。每条内容一个 md 文件（`content/`）,一个 `index.json` 汇总索引,系列元数据在 `series/`。
- **自动存档**：走完文稿模块后自动写入,状态为「待发布」(pending),含创建时间、选题、标题、平台、文稿正文。
- **回填发布数据**：内容发出去后,说"回填 <内容id> 的数据",或直接跑:

  ```bash
  SKILL_DIR="$HOME/.claude/skills/{{ACCOUNT_SLUG}}-workflow"
  [ -d "$SKILL_DIR" ] || SKILL_DIR="$HOME/.codex/skills/{{ACCOUNT_SLUG}}-workflow"
  python3 "$SKILL_DIR/scripts/update_metrics.py" --id 2026-07-27-选题slug \
    --views 125000 --likes 8300 --comments 420 --completion-rate 0.42 \
    --publish-date 2026-07-28 --notes "前3秒钩子有效"
  ```

- **查库**：`query_db.py --search "关键词"`（查是否做过）、`--series "系列名"`（列出某系列各期）、`--top 5 --by views`（按播放排序找可复制的打法）。选题模块会自动查库去重、找系列线索。
- **系列合集**：存档时带 `--series "系列名"` 即把该条归入系列,系列成员与定位记录在 `series/<系列slug>.md`。

## 工作区 workspace

内容资产库是**跨重装的长期库**（结构化去重、系列线索、发布数据回填）；工作区 `workspace/` 是**本账号的当前工作台**——一支视频从选题到成片的完整产出、发布后的后台数据、复盘报告都落在这里。两者并存：文稿模块既往 content-db 存一条档，也往 workspace 落 `视频基础信息.md`。

- **一支视频一个文件夹** `workspace/<YYYY-MM-DD>_<最终标题>/`：`视频基础信息.md`（口播稿+标题候选+终审记录+封面+分镜指针的统一文档，是这支视频的真源）、`封面/`、`参考资料/`、`storyboard_plan.md`（分镜）、`数据/<平台>_数据.md`（发布后）。
- **账号级** `workspace/账号数据/<平台>.md`（发布后由数据追踪脚本刷新）、`workspace/reports/`（复盘报告）。
- **硬规则**：文件夹同名即新建、**绝不覆盖**；抓取数据/多版文稿每轮加后缀留档。

完整目录契约、`视频基础信息.md` 的节次 schema、复盘报告模板见 [工作区目录约定](references/workspace-guide.md)。选题/文稿/标题/封面/分镜/数据六个模块的落盘路径都以它为准。

## 发布后数据追踪

视频发布**之后**追踪 {{DATA_TRACKING_PLATFORMS}} 后台数据的一条工作流（和"把选题变成视频"的创作流程是两件事，可单独触发）。脚本随本技能装在 `scripts/` 下，数据写回本技能自己的 `workspace/`（`scripts/_paths.py` 自动定位，无需传路径）。

**发布后第一件事**：用户给出某平台创作者后台的稿件数据页链接时，先把它回填进该视频 `视频基础信息.md` 的 `创作后台链接（各平台）` 块对应平台行——这是后续「更新数据」的入口。

触发语 → 脚本速查（只列本技能覆盖的平台）：

| 触发语 / 输入 | 脚本 | 落盘 |
|---|---|---|
| "更新账号数据" | `douyin_account.py --save-raw` | `账号数据/抖音.md` |
| "更新小红书账号数据" | `xiaohongshu_account.py --save-raw` | `账号数据/小红书.md` |
| "更新B站账号数据" | `bilibili_account.py --save-raw` | `账号数据/B站.md` |
| "更新视频号数据" | `channels_metrics.py --save-raw` | `账号数据/视频号.md`（账号+全部稿件一次抓全） |
| 给抖音作品详情页链接 | `douyin_metrics.py --url "<链接>" --video "<日期_标题>" --save-raw` | 该视频 `数据/抖音_数据.md` |
| 给小红书笔记数据页链接（含 noteId） | `xiaohongshu_metrics.py --url "<链接>" --video "<日期_标题>" --save-raw` | 该视频 `数据/小红书_数据.md` |
| 给 B站稿件数据页链接（含 BV） | `bilibili_metrics.py --url "<链接>" --video "<日期_标题>" --save-raw` | 该视频 `数据/B站_数据.md` |
| 点名某支视频的视频号数据 | `channels_metrics.py --video "<日期_标题>" --save-raw` | 该视频 `数据/视频号_数据.md` |

**cookie**：各平台会话 cookie 放在本技能根目录 `.douyin_cookie` / `.xiaohongshu_cookie` / `.bilibili_cookie` / `.channels_cookie`（已 gitignore，绝不入库）。过期时（脚本报"未抓到…数据"或 `code!=0`）让用户从浏览器复制对应平台 `document.cookie` 覆盖该文件即可，脚本无需改动。抖音/小红书/视频号走 Playwright（需 `python3 -m playwright install chromium`），B站纯 requests。

接口字段、「更新数据」范围规则、复盘写法见 [发布后数据追踪手册](references/data-tracking.md)。
