# 工作区（workspace）目录约定

本工作流的所有产出都落在技能目录下的 `workspace/`。它是「记录作品 → 追踪数据 → 复盘 → 反哺创作」这条闭环的落脚点：每支视频一个文件夹，账号级数据和复盘报告各有固定位置。选题、文稿、标题、封面、分镜、发布后数据六个模块都往这里写，路径必须一致，后续的数据追踪脚本才找得到。

> 账号：{{ACCOUNT_NAME}}　｜　平台：{{PLATFORMS}}　｜　视频形态：{{VIDEO_FORMAT}}

## 顶层结构

```
workspace/
├── 账号数据/                     # 账号级后台数据（发布后由数据追踪脚本刷新）
│   ├── 抖音.md / 小红书.md / B站.md / 视频号.md   # 覆盖到的平台各一份
│   ├── 总计.md                   # 多平台汇总快照（手工重算，跨平台只看量级）
│   └── raw/                      # 账号级原始接口 JSON 存档（*_raw_*.json）
├── reports/                      # 复盘分析报告
│   └── YYYY-MM-DD_<一句话主题>.md
└── <YYYY-MM-DD>_<最终标题或选题名>/   # 一支视频一个文件夹
```

**账号级文件命名统一为 `<平台>.md`**（B站也写作 `账号数据/B站.md`，不要用别的变体），这样数据追踪脚本落盘和文档索引才对得上。只落地你实际覆盖的平台：{{DATA_TRACKING_PLATFORMS}}。

## 单支视频文件夹

```
<YYYY-MM-DD>_<最终标题>/
├── 视频基础信息.md               # 统一产出文档（见下方 schema），一支视频的真源
├── 参考资料/
│   ├── 参考资料.md               # 用户丢进来的阅读材料（选题第零步建空壳）
│   ├── <来源>-<作者>-<主题>.md   # 精读素材，带来源 URL/作者/日期头
│   └── 抓取数据/                 # web_search / 行情等原始 JSON，每轮加后缀留档
├── 封面/
│   ├── <标题或选题名>_<平台>.png  # 每个覆盖平台一版（如 _bilibili.png / _douyin.png）
├── storyboard_plan.md            # 分镜规划书（花生 AI 成片输入，审查 PASS 后产出）
└── 数据/                         # 发布后各平台后台数据
    ├── <平台>_数据.md            # 稿件级数据（抖音_数据.md / B站_数据.md ……）
    └── 数据_raw/                 # 稿件级原始接口 JSON（<platform>_raw_<id>.json）
```

**硬规则**：
- **文件夹命名 = `<创建日期>_<最终标题>`**；标题未定前先用 `<日期>_<选题名>`。**同名即新建，绝不覆盖**已有文件夹。
- **同一素材多版留档**：想保留另一种框架/写法时，另起 `视频基础信息_<版本名>.md` 并存，不覆盖原文件。抓取数据每轮加后缀（`web_search_阶段二.json`），不要覆盖上一轮。
- **封面文件名带平台后缀**，与 `generate_cover.py` 的 `--platform` 对应。

## `视频基础信息.md` schema（统一产出文档）

一支视频从选题到成片的所有产出都汇进这一个文件，按下面的节次顺序写，模块产出时逐节追加：

```markdown
# 视频基础信息 · <选题名>

## 元信息
- 平台: <主发布平台>（<并行分发平台>）
- 选题方向: <一句话选题角度>
- 最终标题: <确认后的标题>
- 标题状态: ⚠️ 待确认 / ✅ 已确认
- 发布日期: 待填
- 视频链接: 待填
- 创作后台链接（各平台，复盘更新数据用）:
  - 抖音: 待填
  - 小红书: 待填
  - B站: 待填
  - 视频号: 待填
- 封面文件: 见同目录 `封面/`
- 发布后数据: 见同目录 `数据/`

## 一、口播稿
<纯口播文稿，中文标点，不夹画面/章节标注——花生 AI 的唯一驱动源>

## 二、爆款标题（3 个候选）
<3 个带星级与角度说明的候选，选定后标「✅ 最终采用」，并回改开头 hook 兑现该标题>

## 三、终审记录（独立审查）
<按 script-review.md 三层清单逐轮 FAIL/PASS 记录，PASS 才可进封面>

## 四、封面
<设计说明 + 各平台版：采用标题 / 大字 / 英文提示词 / 生成命令 / 成图路径>

## 五、分镜规划（花生 AI）
<指针小节：场景数 / 分镜数 / MG 占比 / 画风一句话 + 逐字校验结论；完整分镜在同目录 storyboard_plan.md>
```

> 「创作后台链接（各平台）」这一块是发布后数据追踪的入口——视频发出去后第一件事就是把各平台创作者后台的稿件详情页链接回填到对应平台行，之后跑「更新数据」时脚本直接从这里读 `--url`，不用再找你要。详见 [数据追踪](data-tracking.md)。

## 复盘报告（`reports/`）

每份报告都是一次「为什么」的追问——为什么这条比那条流量高、为什么吸粉差、为什么被限流——目标是把结论沉淀下来反哺后续创作。命名 `YYYY-MM-DD_<一句话主题>.md`，写法：

```markdown
# <主题>
- 日期：YYYY-MM-DD
- 分析对象：账号 / 某几支稿件
- 数据区间：...

## 一、现象（先摆事实，带数字）
## 二、拆解（为什么，逐条归因，区分事实与推测）
## 三、结论
## 四、可执行的迭代动作
```

数据来源：账号级看 `账号数据/<平台>.md`，稿件级看各视频 `数据/<平台>_数据.md`，要更细回查 `数据_raw/` 原始 JSON。

## 与内容资产库的分工

workspace 是**本账号的工作区**（当前在做/已发布视频的完整产出与后台数据）；内容资产库 `~/.claude|.codex/content-db/{{ACCOUNT_SLUG}}/` 是**跨重装的长期库**（结构化去重、系列线索、发布数据回填）。两者并存：文稿模块既往 workspace 落 `视频基础信息.md`，也往 content-db 存一条档。重装/更新 skill 不影响 content-db 的历史数据。
