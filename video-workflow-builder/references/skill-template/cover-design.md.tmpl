# 封面

> 本模块可单独唤起：说"帮我生成封面""按这个标题做张封面"即可，无需走完整流程；也可以接在标题定稿之后，以选定标题为大字方向生成对应平台封面。

**面向受众**：{{AUDIENCE}}（{{PLATFORMS}}）。**人设**：{{PERSONA}}。

**平台画幅规格**：{{COVER_SIZES}}

---

## 一、1秒法则：封面必须同时传达三件事

观众划到封面的决策时间不到1秒，一张合格封面必须一眼传达：**话题是什么**（核心讨论对象一眼认出）、**什么情绪**（按第三节情绪-配色映射直给）、**一句勾人的大字**（结论/冲突/问题）。三件事缺一不可，动手前先分别想清楚再落成画面。

## 二、封面策略的两层结构：变量层 vs 审美基因层

一张封面由七个维度组成，但它们分属两层，别每期都从零设计：

- **变量层（每期变）**：**文案**、**主视觉**——随本期标题和内容走。
- **审美基因层（一次定稿、每期只小步迭代）**：**构图、配色、材质纹理、字体、设计风格**——是账号识别度的来源，锁定后每期复用。

**关键手法：用"设计语言"一次调动成套自洽审美。** 别一项项去指定配色/字体/材质（外行很难描述、且各维度容易打架），而是认领一种模型已深知的**设计语言**，让五个维度从它自然长出来。本账号的默认设计语言：{{COVER_DESIGN_LANGUAGE}}。需要切换情绪时在此基因上做偏移（如配色点缀色上调），但不换整套骨架。

**自洽性自检（出图前必过）**：配色/材质/字体/构图是不是都指向同一种感觉？有没有哪一项唱反调？有就是设计语言没锚好，回去重锚。

## 三、情绪-配色映射（本账号基因，锁主色板 + 情绪在色板内偏移）

配色第一功能是品类识别（一眼认出"讲什么的"），其次准确传情绪：

{{COVER_EMOTION_MAP}}

唯一判断标准：配色是否准确传达了这条内容想让受众感受到的情绪与品类，不是"惯例用什么色"。

## 四、唯一视觉锤与留白

一个主视觉元素占据最抓眼位置，不堆砌多个同等重量元素；主体不铺满画面，刻意留干净区给大字；两者冲突时优先保大字清晰可读。

## 五、大字文案：4-8字，用受众口语

理想4-8字，短了信息量不够、长了竖屏缩略图挤成一团。一句大字只表达一个判断或一个情绪冲击点，多层信息拆到主锤字+副标题两级。用受众口语（参考{{AUDIENCE_VOICE}}这类表达），不用书面总结。

## 六、主视觉从哪来：真实素材图生图 还是 纯文生图（关键判断）

主视觉"溶解进背景、不抓人"是纯文生图的通病——它画的是氛围不是符号。判据只有一条：**这个主体有没有"观众会拿去和现实核对的标准答案"**。

- **具名实体**（这位特定人物本人、这一款特定型号、这座特定地标）→ 有标准答案、受众会核对 → **喂真实素材走图生图**（`--reference-image`）。纯文生图对"特定的谁/哪一款"必然编错（脸不像、型号糊成通用款），一眼被抓包。
- **泛指类别**（一般意义的场景/物件/人群）+ **抽象概念**（某种趋势/情绪/机制）→ 无标准答案 → **纯文生图**，模型自选意象反而好。
- **精度线随受众专业度上浮**：受众越懂这个领域，对具名实体的核对越狠，越要往"喂真图"这侧靠。

> **图生图是"忠实渲染器"**：喂什么还原什么，所以主体不溶解；但喂错就把错误也精美地还原（找错的底图，它只会把错的那个画得更像样）。所以选源精度靠"核得住"不靠"搜得准"，必须在拿到候选图和喂进模型之间插一道核验（见第七节）。

## 七、参考图选源与核验闸（绑定半自动/全自动模式）

核验本质不是"调识图模型"，而是"谁在场谁负责"：

- **全自动模式 → 降档纯生成**：没人核对就不拿未核验的搜索图跑图生图（防"找错还渲染得漂亮"）。宁可主体发虚，不可张冠李戴。
- **半自动模式 → 两条路，(a) 优先**：
  - **(a) 用户自带参考图（推荐默认）**：既躲开图搜反爬/脏标注，又零选源风险。
  - **(b) 自动搜图 + 人工确认源**：把候选图连同判别特征亮给用户点头（如"这应该是X：特征1+特征2，对吗"）再往下。
- **识图模型仅为可选增强**：环境模型能看图就用它预过滤候选，看不了（如纯文本模型）就直接上人工。**人工确认是地板。** 每个"调模型"步骤都要有纯文本降级路径，不默认用户都有识图模型。

## 八、主权合规硬约束（红线，二值不可协商）

与文稿审查"港澳台不与中国并列"是一对——那条管文字，这条管画面。普通观众未必注意，但出错即重大事故，由硬约束兜底、不靠审美判断：

1. **触发 = 任何提及即触发**：文稿只要出现**台湾、香港、澳门、新疆、西藏**（哪怕一句背景），即触发第2条。
2. **涉敏题封面禁止出现**：**旗帜、国徽、任何人物**（不限相关人物，任何人都不放）。这些题导向中性概念/场景视觉，从主视觉候选里直接剪掉旗帜/国徽/人物。
3. **地图全局禁令（所有封面，不限涉敏题）**：一律禁止出现地图。AI 生成地图边界不可靠（南海九段线、藏南、台湾着色极易出错），任何题材都是事故温床。凡"世界地图底纹/地图连线"一律作废，改用棋局、连线网络、光影纵深、去标识的场景等非地图意象。

出图前先过这三条，任一触碰即改。

## 九、文案与标题互补，不重复同一句话

封面大字和标题是两块广告位，合起来信息量要大于任一单独一块。不要把标题原样搬上封面再读一遍。自查：把标题和封面大字放一起读，若比只读标题没有增量就是重复了；方向也不能打架。

## 十、系列感：固定排布（一次定稿、每期复用）

{{COVER_SERIES_LAYOUT}}

换排布成本极高（等于重新培养识别习惯），确定后每期直接复用，不临时改——这是账号识别度最强的锚点。

## 十一、生成脚本调用

提示词写前先明确：**大字用的就是用户确认的标题（或只做提炼不改立场的精炼版）**，再定情绪色调（第三节映射）和主视觉。**先按第六节判断主视觉走图生图还是纯文生图、按第八节过一遍红线。** 提示词四块组织：① 画面主体（英文描述，对应第二节认领的设计语言）；② 图上大字（中文逐字写出，"图中央用醒目大号中文写：'XXXX'；下方小一号：'YYYY'"）；③ 风格词（对应设计语言）；④ 排版要求（文字清晰不被遮挡、主体不占满、留白、单一视觉焦点、文字与主体控制在中央区、四周留可裁切余量）。

**纯文生图**（泛指/抽象主体）：

```bash
python3 scripts/generate_cover.py --platform <平台> --prompt "英文画面描述+中文大字指令+风格词+排版要求" --output <选题关键词>_<平台>.png
```

**图生图**（具名实体，先按第七节核验参考图确实是对的那一个）：

```bash
python3 scripts/generate_cover.py --platform <平台> --prompt "..." --reference-image 已核验的真实素材.jpg --output <选题关键词>_<平台>.png
```

具体 `--platform` 取值和画幅以{{COVER_SIZES}}为准；多平台同选题同步出图、各按画幅重新构图而非裁切；文件名用选题/主体关键词命名，方便长期归档。

## 十二、生成后检查（不可跳过）

1. **中文文字正确**：gpt-image-2 生成中文偶尔错别字/缺字/糊字，逐字核对图上文字与提示词一致、清晰可读，不一致就改提示词重出。
2. **红线复核**：涉敏题图上确无旗帜/国徽/人物；任何题图上确无地图。
3. **固定元素位置**：{{COVER_SERIES_LAYOUT}}要求的账号名/logo/角标在约定位置，漂移就重出。
4. **大字与标题不打架**：封面立场和标题一致（不能标题看空、封面显得看多）。

## 密钥安全（红线，不可违反）

- 真实 API key 只能写进产物目录下、被 `.gitignore` 排除的真实 `.env` 文件里，由用户自己配置。
- 随产物一起提交/追踪的 `.env.example` 永远只放占位符，绝不写入真实密钥值。
- **绝不硬编码真实 API key** 到 `generate_cover.py`、SKILL.md 或任何会被版本控制追踪的文件中。
- **绝不提交 `.env`**——确认 `.gitignore` 已排除该文件，运行前检查一遍。
